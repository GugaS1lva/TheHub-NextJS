export default function RotaTeste02() {
  return (
    <div>
        <h2>Mais uma rota de teste. Isso aqui será uma página estática até que se (ou eu) prove o contrário</h2>

        <p>Essa rota aqui não tem layout. Se quiser criar, basta criar um arquivo chamado <code>layout.tsx</code> e definir os estilos de layout dentro dela.</p>
    </div>
  )
}